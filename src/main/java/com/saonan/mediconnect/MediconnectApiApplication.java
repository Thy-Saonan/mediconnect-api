package com.saonan.mediconnect;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MediconnectApiApplication {
  // This is a very long comment that goes way beyond the normal line length limit
  // and should trigger a checkstyle violation right here
  public static void main(String[] args) {
    SpringApplication.run(MediconnectApiApplication.class, args);
  }
}
